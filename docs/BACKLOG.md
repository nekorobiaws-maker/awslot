# JAWSLOT -ジョースロット- 作業バックログ(2026-08-16 更新)

ユーザー指示の未消化分。着手時はこのファイルの該当項目を根拠にし、完了したら [x] を付ける。

> 表示名は **JAWSLOT -ジョースロット-**(U45 / 2026-08-15)。
> コード上の識別子・内部名(`window.AWSLOT` / CSS の id・class / ディレクトリ名 /
> 環境変数 `AWSLOT_AMA`)は AWSLOT のまま変えない。
> 改名するのは**プレイヤーに見える文言と、人が読む文書のタイトル**まで
> (2026-08-15 検証指摘 F20 で docs/ の見出しと画像生成プロンプトも改名済み)。

## 進行中
- [x] 前兆・予告ワークフロー: 前兆システム+予告3カテゴリ+統合+検証
  - 前兆32パターン(`data/zencho.js`)/ 擬似連2種 / 予告シナリオ **520本**(`data/scenarios/**` 16ファイル)/
    赤文字予兆の信頼度設計(U73 で 75〜85% に着地)まで完了。実測プローブは
    `zencho-freeze-probe.mjs`(前兆の総量・フリーズ)と `hot-trust-probe.mjs`(赤の信頼度)の2本
- [ ] UI画像素材のコード組み込み(実行中): 筐体/リール部品/ステージ背景/ロゴ帯/下部パネル。操作系は筐体絵の描き込み位置にDOMヒットエリアを合わせる

## M: メカニクス改修(スコアアタック化+バランス) — ワークフロー完了後
- [x] **50回転固定のスコアアタック化**: 全モードの回転を通算50Gでカウントし終了。終了時にART/ボーナスの残りG数・ストックを「残G×現在純増」で買い取ってクレジット加算→最終スコア確定。リザルト状態の新設(描画はP側)
- [x] 天井は「30G消化で未当選ならCZ確定」に読み替え(元指示は100Gだが50回転と不整合のため。SLA/999G名義は廃止)
- [x] 初当りを軽く: 目安1/40〜50(50回転中に「何も起きない回」を減らす)
- [x] 小役アップ: ベル1/6→1/5前後、レア役合計1/33→1/25前後。コイン持ち改善
- [x] 上乗せ再設計: G数上乗せは控えめ(+10G程度まで)、純増ブースト(DC/倍率)重視でスピード感
- [x] ボーナス改名: S3 BIG→**ゴーストボーナス**、Lambda REG→**シャークボーナス**、DynamoDB BIG→**ゴーストボーナスSP**(仮称)。データ・シナリオ・LCD・README全体で統一
- [x] ゴーストボーナス10G/SP 1セット10G/シャーク6G(データ適用済み 2026-08-13)
- [x] バランス指標は機械割ではなく「50回転の平均スコア200〜300枚・上振れ1000枚超」の分散設計。シミュ実測を報告

### M の実測(2026-08-13 / 50回転×10,000セッション×2シード)
| 指標 | 実測 |
|---|---|
| 平均スコア | 229.8 / 230.4枚(目標200〜300) |
| 中央値 | 172 / 170枚 |
| 上位1% | 981 / 1,027枚(目標1000枚超) |
| 最高 | 1,666 / 1,493枚 |
| ボーナス遭遇 | 1.02回/セッション(未遭遇 0.87%) |
| RUSH終了時の平均DC | 5.20 / DC8到達 8.3%のセッション |
| 買い取り発生 | 92.5% / 平均75.9枚 |

- 追加実装: RESULTモード(描画はP2)、Rキー/↑キーでのリスタート、矢印キー操作
- ALARM役の表示名を Bedrock に、BLANK のモチーフを SES に変更
- sim.mjs に `--session=N` を追加。入賞待ちの揃いを小役統計から除外(GHOST 1/604 バグ修正)

## P: 演出・見た目調整パック — ワークフロー完了後
- [x] **テロップ可読性**: 最低表示時間+フェードアウト、重要告知(CZ突入/ボーナス確定/上乗せ/昇格)は次レバーONまで残す。エンジンレベル(`staging/anims/lcdanims.js`)で実装済み
  - 実効文字数から最低表示時間を導出(短文1.6秒 〜 長文2.8秒)。順番待ちのキューと sticky 告知を持ち、
    レバーONだけが最低表示時間より優先して次へ進める
  - 実測は `scripts/text-hold-probe.mjs`(本物の Timeline / LcdAnims を1/60秒ずつ回して「何秒読めるか」を測る)
- [x] **Bedrockタイピング予告**: 文言を期待度別に15種程度へ拡充。**「BONUS」を含む文言は当選ゲーム限定**(=画面にBONUSと出たら本当に確定)。ガセには惜しい文言のみ
  - 文言23種(弱6/中6/ガセ6/激アツ5)を `BEDROCK_LINES` へ配列化し、新LCDアニメ `bedrock_typing` が演出用RNGで抽選。ゲーム抽選RNGは非消費
  - BONUS入りは `tier:'hot'` のみ。`pickBedrockLine()` が hot 以外で BONUS 入り文言を機械的に除外し、hot を渡すシナリオは当選確定イベント(`zencho_end`/ENTRY と `BONUS_READY` のレバーON)の2本だけ
- [ ] **ステージ再設計**: Free Tier/SLA/999G表記を廃止し、通常/高確/激アツの3ステージに。AWSモチーフで見た瞬間に分かる演出(例: 平和なリージョン→トラフィック急増でオートスケール準備→War Room全リージョン警戒)。assets/ui/stage_*.png を活用
- [x] **AWSクイズルーレット演出**: 出題→4択ルーレット→当選時のみ正解に止まりCZ移行、非当選は不正解でガセ。問題10問程度(キャッシュ→ElastiCache、DNS→Route 53、アーカイブ→Glacier、NoSQL→DynamoDB、CDN→CloudFront、負荷分散→ALB、サーバーレス→Lambda、オブジェクト保存→S3など)。新LCDアニメ+データ定義
  - `src/data/quiz.js`(**46問** / U52a で +21問。U46a で衛星の1問を削除)+ 新LCDアニメ `aws_quiz_roulette` + `src/data/scenarios/quiz.js`
  - 正解版は CZ の `modeEnter`(= 突入確定後にしか来ない)、不正解版は前兆の `zencho_end`/MISS(= 非当選確定)にしか貼っていないため、「正解に止まった=当選」が when 条件だけで保証される
  - 実測(4,000セッション): CZ突入告知の 40.3%(約2.5回に1回)がクイズ経由 / ガセは終了告知の 32.4%
- [x] リザルト画面の描画(最終クレジット・買い取り内訳・もう一回ボタン) — `src/render/resultpanel.js`。
  スコア / RANK / 称号 / 買い取り明細(`buyout > 0` のときだけ)/ 学習サマリーの折りたたみまで実装済み

## 第2ラウンド(2026-08-13午後、進行中)
- [ ] 筐体: cabinet2載せ替え+ディスプレイ座標合わせ(横長化)+HUDをリール直下+リザルト画面(スコア/名前入力欄/殿堂入りボタン見た目のみ/中央にもう一度ボタン)+lcd表記削除(FREE TIER/SLAゲージ)+ステージ名を背景に追従+幽霊左右うろうろ+テロップ重複抑制(main側)+CZ ui:'sfn'画面
  - ※ このうち **リザルト画面 / テロップ重複抑制 / CZ ui:'sfn' 画面**は個別項目として完了済み(下記)。残っているのは筐体絵の載せ替えと座標合わせ
- [x] クイズルーレット: リール停止同期の3段進行 → **U53 で「最初に止めるリール」3択に差し替わって解決**。
  第1停止がそのまま答えになるので、停止と演出が構造的に同期する(`data/scenarios/quiz.js` の `qz_reelpick_*`)
- [x] テキスト帯: 同カテゴリ文言の重複排除(「ボーナス確定」が2〜3個出る問題)+`getVisibleTexts()` —
  `staging/anims/lcdanims.js` に `getVisibleTexts()` / `isDuplicateText()` を実装。
  完全一致だけでなく**同カテゴリ**も重複とみなす(モードのルール文だけは完全一致判定に切り替えられる)
- [x] CZ改修: Trusted Advisor 全緑=ボーナス確定の見せ方 / 新CZ「Step Functions CZ」(流れきったらボーナス)。
  どちらも実装済み(`SFN_CZ` は現行11種の7番目)
- [x] 演出済み: ルーレット減速+大型化 / RUSH突入スラム(3変種) / Bedrock揃い=LLMイベント100%発火
- [x] 演出量産第2弾(未登場サービス): AI分析系 / セキュリティNW系 / インフラ・ロマン系。
  `yokoku-ai.js` / `yokoku-secnet.js` / `yokoku-infra.js` ほか **16ファイル・シナリオ総数 520本**として登録済み
- [x] テロップ寿命: 前ゲームの「次に期待」等はレバーONで消える(flow.js)
- [x] 絵柄: 新9種を中央揃えで再切り出し、コンパス絵柄廃止(REPLAY2=DynamoDB絵柄に統一)、ステージ名を会場名に統一

## 第3ラウンド(2026-08-14〜15 のユーザー指示。U番号はその日の指示の通し番号)

### メカニクス(実装済み。データの正は各 `src/data/*.js`)
- [x] **U32 引き戻しの作り直し**: 引き戻しは **ホットスタンバイ(Multi-AZ)の1段のみ**(5G / 成功率55% / レア役成立で+1G、上限15G)。
  成功時の復帰先が「元のRUSH」から **復旧のゴーストボーナス**(`data/rushes.js` の `RECOVERY_BONUS` = `S3_BIG`)に変わった。
  = 引き戻し成功はRUSH確定ではなく、そのボーナス中にレア役を引けるか(45%)でRUSHに戻る。
  Route 53 フェイルオーバーは `retired: true`(`?mode=ROUTE53_FAILOVER` の直撃デバッグ専用)
- [x] **U40 ヒーローRUSHの再配分**: 毎ゲーム 1/2 の当選を **100 → 50クレジット** へ。
  レア役の +α は据え置きなので「レア役を引けた瞬間が勝負」が4種で揃う。期待獲得 358 → **233枚**
  (`RUSH_EXPECTED_GAIN.HERO_RUSH`。買い取り計算もここを見る)
- [x] **U44 甘スロ(`?ama=1`)**: 通常時テーブルの**レア役だけ2倍**にした救済設定。
  ボーナス中のテーブルと RUSH当選率(12/45/85%)は通常と同一 = 甘くなるのは「当たるまで」だけ。
  切替はURLクエリ(再読み込み)のみでランタイム切替は作らない。node からは `--ama` / `AWSLOT_AMA=1`。
  実測 初当り 1/61.8・機械割 221%(通常は 1/103前後・181〜183%)
- [x] U45 表示名を **JAWSLOT -ジョースロット-** へ(見える文言のみ。コード識別子は AWSLOT のまま)

### 演出・コンテンツ(2026-08-15)
- [x] **U52a クイズ +21問(25 → 46問)**: Redshift/EFS/SQS/EventBridge/Step Functions/CloudTrail/X-Ray/GuardDuty/
  Auto Scaling/Organizations/Systems Manager/Fargate/ECR/CodePipeline/API Gateway/Rekognition/Transcribe/
  SageMaker/Bedrock/Direct Connect/NAT Gateway。既存問題と正解サービス・論点が重複しないことを確認済み
- [x] **U52b 予兆 +10ネタ**(`yokoku-batch4.js` / yb4_ 16本): Fargate/ECR/EFS/Service Quotas/Data Firehose/
  DAX/WorkSpaces/Audit Manager/NLB/Organizations。弱=ガセ・中=レア役限定、文字は全て座布団つき lcd.text
- [x] **U46a 衛星・アンテナネタの削除**: Ground Station 予告3本 + クイズ1問を削除(重みは同プールへ自動按分)
- [x] **U46b Bedrock 生成演出のパターン追加**(`yokoku-bedrock.js` / ybr_ 6本 + クイズ導入):
  IAMが設定されます=チェリー / スイカの美味しい季節ですね=スイカ / チャンスかもしれません=チャンス目 /
  サメの群れが近づいています=サメ揃い / クイズの時間です=クイズ導入 / 文字化け・空出力=**ハズレ専用**
  - タイピング盤面(`bedrock_typing`)の文言は staging 側の `BEDROCK_LINES` が持っていてシナリオから渡せないため、
    生成結果は `bedrock_boot`(生成中の画)+ `lcd.text`(結果の1行)で出している。
    盤面の中で出したくなったら演出担当が結果対応の tier を `BEDROCK_LINES` に足す
- [x] 検証の指摘対応: 弱予告が天井到達ゲームを除外(弱=ガセの保証)/ `yb3_shield_zencho` の重み 70→45 /
  `bonus_end_quiet` が100回転切れ・エンディング・リスタートの畳み退出で鳴らないように条件追加

### メカニクス第2弾(2026-08-15 / U52c・U48)
- [x] **U52c CZを3種追加(8 → 11種)**: `CONFIG_RULES`(AWS Config 準拠ルールCZ / 4G / 36% / ★☆☆)・
  `DX_REDUNDANCY`(Direct Connect 冗長化CZ / 4G / 47% / ★★☆)・
  `SHIELD_DDOS`(Shield / WAF DDoS 防御CZ / 3G / 66% / ★★☆)。
  振り分けを11種へ再配分(加重平均突破率 0.4186 → **0.4134** / 平均滞在 4.06G → **3.91G** = 初当りは不変)。
  進行の型は既存とずらしてある(Config=1ルールずつ確定する打ち切り型 / DX=当落先付けの一斉開通型 /
  Shield=1ゲーム2波の波状耐久)。目標宣言(`goal` / `goalDetail`)は spec 側に持たせた
  - **液晶の盤面は既存の描画を借りている**(ui: checklist / pillars / fis)。
    `src/render/**` が変更禁止だったため、**固定文字が題材と矛盾しない盤面だけ**を選んで流用した。
    専用盤面を起こすときは ui を新IDにして `render/lcd.js` の `_drawCz` に足すこと(演出担当への申し送り)
- [x] **U48 レア役の基準を2倍**(`data/flags.js`): 弱チェ 1/25 / スイカ 1/50 / チャンス目 1/90 /
  強チェ 1/125 / サメ 1/600 / ゴースト 1/3000(合計 **1/12.3**)。甘スロは更に2倍(従来比4倍)
  - 相殺: CZ当選 0.5倍 / ステージ昇格 0.7倍 / czPerGame 0.6倍 / フリーズ率(レア役)0.5倍 /
    AS上乗せ台数・CF確定クレジット・AuroraのACU上げ幅・ヒーロー+α・派生ゾーン当選を約0.5倍 /
    Well-Architected CZ を 10G → **9G**(6本積める確率 89.9% → 85.0%)
  - 実測(4,000セッション × 3シード): 初当り **1/96〜99** / 平均 **250〜259枚** / 中央値 132〜146 /
    上位1% 1,440〜1,724 / 機械割 **183〜186%** / フリーズ 9.4〜9.7%。甘スロは 1/60・232%
- [x] 残minor(担当分): 引き戻し `resumeStock` の非対称(買い取りをやめて復帰と一致させ、
  「上位ATはストックを持ったまま転落しない」を sim の検証に追加)/ `RESUME_SOURCES`・`_countTransition`
  のコメント陳腐化(U32 で復帰先がボーナスになった旨)/ U40陳腐化(lottery.js・modes/rushes.js)/
  Kinesis の note が 100枚のままだった箇所を 150枚へ
- [ ] **ルナカメオの頻度按分(未対応 / 担当範囲外)**: U48 でレア役が2倍になったので
  `src/data/scenarios/yokoku-luna.js` の chance をそのままにすると **1/540 → 1/272** になる。
  1/400〜600 を維持するには chance を半減する(スイカ 0.040→0.020 / 弱チェ 0.030→0.015 /
  強チェ 0.060→0.030 / チャンス目 0.045→0.023 / 確定役 0.350→0.175)。
  `scenarios/**` は今回の変更禁止範囲だったため未着手
- [x] エンディング発生が **0.22%/セッション**(目安 0.3〜0.5% にわずかに届かない)。
  → **U50(2026-08-15)で差枚条件を 2222 → 1500枚へ下げて解決**(実測 **0.41%**)。
  申し送りどおり `atSetCount`(4のまま)ではなく差枚側を触っている。
  U50 で分布の上を圧縮した結果、2222枚は 20,000セッションで 0.01% = 到達不能になっていた

### U79 予兆の絵が偏る(2026-08-16 / ユーザー体感「前兆で同じ演出ばかり出る」)
32本ある予兆パターンのうち、実際にプレイヤーへ届いていたのは上位の十数本だけだった。

- [x] **原因は重みの配り方**。U58 で18本足したとき「グループ合計を据え置く」ために
  新規は既存の隙間(`real 5〜6` / `fake 3〜8`)へ入れたので、主力(`cloudtrail` real 52)との差が
  10倍前後ついたまま残っていた。実測(2,000セッション / 200,000G / 5,165回の前兆)で
  **上位5本が36%・下位11本は1%未満・直前と同じパターンが4.5%**
- [x] **重みは1つも書き換えず、抽選の直前に2つの倍率を掛ける**方式で解消
  (`data/zencho.js` の `ZENCHO.variety` / `game/modes/freetier.js` の `buildVarietyTable`)。
  **1. 露出の平準化**(重み合計の -0.7 乗 = よく出るものほど控えめに)/
  **2. 直近6回の履歴ペナルティ**(直前に出たものは ×0.05)
- [x] **期待度・当落は完全に不変**。倍率は本前兆とガセへ**同じ値**を掛け、掛けたあと
  「擬似連 / 赤文字持ち / それ以外」の**クラスごとの重み合計を元へ戻す**ので、
  パターンごとの信頼度(real:fake 比)・擬似連が選ばれる確率・赤文字予兆の割合が動かない。
  `drawWeightedFixed` を通す形も変えていないので **RNG の消費数も1回のまま**
- [x] **実測**: 上位5占有 36.07% → **28.69%** / 最少 0.74% → **1.36%** /
  直前と同じ 4.47% → **1.47%**。擬似連の選出は **736回 / 14.25% で before と1回も違わない**。
  `sim.mjs`(200,000G)・`sim.mjs --session=3000`・`balance-probe.mjs` の
  **出力が before と完全一致**(= 出目・機械割・初当りは1ミリも動いていない)
- [x] 赤文字予兆の台帳を `data/zencho.js` の `ZENCHO_HOT_PATTERNS` に新設。
  `scenarios/zencho.js` へ `zn_hot_*` を足したらこちらにも1行足すこと(突き合わせ用のコマンドは同所のコメント)
- [x] 確認口: `AWSLOT.recentZenchoPatterns()` で直近に出た予兆パターンを新しい順に見られる

### U76 ヒーローRUSH の表示と当選率(2026-08-15 / ユーザー指示2件)
「HERO RUSH の文字が二重に出ている」「毎ゲームの抽選を 50% × 50枚に」の2件。

- [x] **「HERO RUSH」は盤面の大ロゴ1系統に**(Q2 の GHOST BONUS SP と同じ流儀)。
  突入ポップアップ(`data/scenarios/rushes.js` の `hero_rush_entry`)が
  `HERO RUSH 突入!!` + `5G限定 — 毎ゲーム 80% で 70枚` を出しており、
  **盤面の大ロゴ(y152)と仕様行(y52)の両方と重なって**いた
  (テキスト帯は液晶中心 +44px = y152〜236 を下敷きごと占有するため、
  ロゴは帯の裏に沈んで潰れていた)。常設側を残してポップアップを
  **`AWS Hero に選出!!` の1行(sub なし)** へ。
  併せて `render/lcd-rush.js` の `drawHeroRush` が冒頭の契約3
  「帯が出ている間は常設ラベルを伏せる」を守っていなかったので `textActive` で伏せる
  (終了系ポップアップ `rush_end_hero` / `_zero` の名前はモードの締めなので据え置き。
  その間もロゴが伏せられているため二重にならない)
- [x] 他3種の点検: AS / CF / Aurora の盤面はモード名を**1文字も描いていない**
  (見出しは `EC2 INSTANCES` / `EDGE LOCATIONS` / `AURORA CAPACITY UNIT` = 軸の名前)ので
  同種の重複は無し。名前を出すのは突入ポップアップだけ = 元から1系統
- [x] **毎ゲームの抽選 80% × 70枚 → 50% × 50枚**(`data/rushes.js` の `hitRate`/`hitCoin`)。
  骨格 56 → **25枚/G**。表示は `heroHitLabel()` 経由なので液晶・テロップとも自動追従
- [x] `hitCoin` が下がって100枚の壁に余白ができたぶん、**非確定役の +α を U63 の半減前へ戻す**
  (弱チェ10 / スイカ・チャンス目18 / 強チェ30。確定役300/500は据え置き)。
  1ゲーム最大 50 + 30 = **80枚**(壁の内側)/ +α は 2.53 → **3.73枚/G**
- [x] `RUSH_EXPECTED_GAIN.HERO_RUSH` 289 → **144**(買い取りもここを見る)/
  `expectedPerGame` 57.9 → **28.7** / README・DESIGN・FLOW 追従
- [x] 実測(3,000セッション × 3シード): 平均 **251.5〜256.9枚**(目標220〜340)/
  中央値 **131〜145枚** / 上位1% **1,216〜1,265枚** / 機械割 **183.8〜185.6%** /
  RUSH 1回は AS 255-285/341 · CF 281/285 · Aurora 276-289/312 ·
  **ヒーロー 118-150/132-152**(中央値/平均)。
  `sim.mjs --session` PASS / `compare-drivers.mjs` 全ドライバ一致
- [ ] **ヒーローは4種で唯一「1回の価値」の目標帯(平均250〜400枚)から外れている**(平均144枚)。
  骨格を戻したまま平均を上げるには +α を1回100枚超にする(=1ゲーム100枚の壁を破る)か
  `games` を伸ばす(=5G限定の一発勝負という正体が消える)しかないため、
  **瞬発プレミアとして意図的に軽い**という整理で着地させた。
  中央値130枚に対し p99 710枚(倍率5.5倍は4種で最大)なので、上振れの尻尾は残っている。
  なお期待値では **プレミア振り分け(ヒーロー1/4)が通常振り分けより約13%軽い**
  (320 → 278枚)。振り分けは据え置いたので、気になるならユーザー判断で
  `RUSH_TYPES.premiumDistribution` か `HERO_RUSH.games` を触る

### U72 CZ導線の作り直し「チャンス目が出たらチャンスゾーン」(2026-08-15 / ユーザー指示3件)
「CZにほぼ入らなくなった」「擬似連が発生しなくなってる」「AS RUSH の純増を15枚に」を**一体で**着地させた回。

- [x] **チャンス目(1/45)= CZ突入確定**(`CZ_ENTRY.table.CHANCE.cz = 1.000`)。
  ステージも内部状態も問わない。**サメ揃い(1/300)も確定**にして
  「チャンス目より重い役でCZに入れない」という倒立を防いだ
- [x] **他のレア役のCZ抽選は全廃**(Bedrock役・弱チェリー・スイカ・強チェリー → ステージ昇格専任)。
  ステージの毎G抽選は主線を食わない「おまけ」へ(高確 1/61.7G → **1/333G** / 激アツ 1/15.6G → **1/83G**)、
  ステージ抽選からのボーナス直撃(6% / 16%)は廃止。天井 78G は救済として維持
- [x] **告知は「チャンス目 — CHANCE ZONE 確定!!」**(`freetier.js` の `CZ_CONFIRM_TELOP`)。
  これが新ルールをプレイヤーへ伝える唯一の場所なので、昇格テロップより優先して出す
- [x] **擬似連の生存**(ユーザー指摘)。確定役をCZへ直結させると本前兆が走らず擬似連が構造的に消える。
  → 確定告知のあと **2〜3Gの短い移行前兆** を挟む形にし、本前兆側の擬似連の重み 10 → **36**。
  実測 **0.185回/セッション(5.4回に1回)→ 0.36回/セッション(2.7回に1回)**。
  原因は「chain の仕様破損」ではなく **前兆の本数 × 擬似連が選ばれる確率(約4%)が元から薄かった**こと
- [x] **オートスケーリングRUSH の純増 35 → 15枚/G**(ユーザー指示)。
  `payoutPerGame` は純増なので払い出しは18枚。薄くなったぶんは**ゲーム数で返す**
  (初期台数 平均 6.96 → **18.59台** / 通算上限 22 → **52G** = 一撃 780枚 ≦ 800枚の壁)。
  AS 1回は 中央値 **270枚** / 平均 **335枚** / p99 **780枚**(目標 150〜300 / 250〜400 / 800以下)
- [x] **再着地**: CZ突破率ラダーを一律 **0.77倍**(加重平均 41.3% → 33.6%。振り分け・★・順序は不変)、
  上位1%の補填に **ボーナス中のベル 15 → 18枚**(純増 8.45 → 10.60枚/G)。
  実測(50,000セッション)平均 **260枚** / 中央値 **150枚** / 上位1% **1,273枚** / 機械割 **186.8%** /
  抽選由来の初当り **1/96.1** / CZ突入 **2.16回**(うちチャンス目 65%)
- [ ] 申し送り: `src/data/ama.js` の `AMA.measured`(初当り 1/62 等)は U72 前の値のまま。
  コードからは未参照だが、表示に使うときは 1/53・平均405枚・中央値250枚・機械割235% へ更新すること
- [x] 申し送り: `scenarios/yokoku-heavy.js` のコメントにある「ALARM は cz 0.14〜」「MELON cz 0.50 / CHANCE cz 0.60」は
  U72 で **ALARM/MELON は 0・CHANCE は 1.000** になった。赤文字予兆の信頼度も測り直すこと
  → **U73(2026-08-16)で完了**。実測は上振れではなく **64.9% への下振れ**だった(理由は下の U73 の節)。
  再設計して **79.3%**(目標 75〜85%)へ着地させ、コメントも全部書き直した

### U73 赤文字予兆の信頼度 再設計(2026-08-16)
ユーザー指定の設計目標は **「赤文字(赤帯)= 信頼度 75〜85%」**。U72 でこれが崩れていたので測り直して組み直した回。

- [x] **実測プローブを新設**(`scripts/hot-trust-probe.mjs`)。演出システム(StagingDirector + Timeline +
  createActions)を丸ごと載せてセッションを回し、**液晶に `tone:'hot'` が出た瞬間**を1回の赤として数えて
  その赤が当たったかを追う。判定は「その赤に紐づく前兆の `zencho_end`」「同じゲームのフリーズ/天井」
  「次ゲーム開始時に前兆が走っていない = 裏切り」の3択で、**数ゲーム後の別当選を的中に数えない**
  (数えると裏切り枠が 3% → 15% に化けて設計が測れなくなる)
- [x] **`--human`(人が打つ間合い)を必須にした**。即押しだとレバーON契機の予告が液晶の告知枠を握ったまま
  前兆の赤が来て、交通整理(`director` の `DROP_TEXT`)で **前兆の赤が 1/4 に落ちる**。
  実機は停止まで2〜4秒かかるのでこの取りこぼしは起きない。**判定は必ず `--human` を正とすること**
- [x] **before の実測(3,600セッション / 360,000G)は 64.9%** で、目標を10ポイント以上下回っていた。原因は2つ:
  1. 本前兆の主流が「確定役の移行前兆(=100%)」になり、前兆の赤(`zn_hot_*`)が **93%** まで上昇
  2. スイカに出していた赤が **CZへ繋がる道を失って構造的にほぼ必ず裏切る**ようになった(実測 1%)
  さらに副産物として、`zn_final_push`(段階3 + 残り1G)が「最も信頼できるサイン」ではなく
  **一番低い赤(52%)**に反転していた(確定役の移行前兆は最長3Gで畳まれるので、そこまで伸びない)
- [x] **再設計: 赤の意味を4つに絞り、1本のシナリオに1つの意味だけを持たせる**
  (`scenarios/yokoku-heavy.js` / `yokoku-wind.js` / `zencho.js` / `data/zencho.js`)
  | 系統 | シナリオ | 占有率 | 信頼度 |
  |---|---|---|---|
  | 前兆の赤 | `zn_hot_*`(16本) | 55.9% | 93% |
  | 伸びきった前兆 | `zn_final_push` | 10.3% | 52% |
  | 確定の先出し | `yh_hot_bedrock_alert`(チャンス目 / サメ揃い) | 13.2% | 100% |
  | 前兆の重なり | `yh_hot_guardduty_alert`(前兆中のレア役) | 10.3% | 82% |
  | 裏切り枠 | `yh_hot_false_alarm` / `yw_hot_false_evacuation` | 10.3% | 1% |
- [x] 動かしたノブは3つだけ。**スイカ単独の赤を廃止**(`yh_hot_guardduty_alert` を「前兆中のレア役」へ) /
  **`yh_hot_bedrock_alert` をCZ確定役専用にして重み 420 → 1,100** /
  **裏切り枠の chance 0.020 → 0.013**(赤全体の 15% → 10%)。
  `yh_hot_guardduty_alert` の重み 420 → 2,100 は **信頼度が全体平均とほぼ同じ**なので、
  平均を動かさずに赤の量だけを戻すために使っている
- [x] **after の実測 79.3%(目標 75〜85% の内側)**。別シード3本でも **80.5%**。
  赤の出現量は **1.14 → 1.08回/セッション**(-5% = 見せ場は維持)
- [x] **ゲーム側の数値には一切触れていない**。CZ導線・レア役出現率・RUSH・ボーナス・前兆の重みは全部据え置きで、
  動かしたのは演出抽選(発火条件・weight・chance)だけ。
  `sim.mjs --session=500` PASS / `compare-drivers.mjs` 4ドライバ全一致 / `validateScenarios` OK
- [x] 設計の全体像を `data/zencho.js` の `ZENCHO_TEXT_COLORS` の上に台帳として書いた。
  **赤を1本でも足し引きしたらここを読んで測り直すこと**
- [ ] **甘スロ(`?ama=1`)は 88%** と目標帯より高い(U73前は 74.5%)。
  レア役2倍で**前兆に占める本前兆の割合そのもの**が上がるためで、標準設定を 75〜85% に置く限り避けられない。
  救済モードの性格として許容しているが、揃えたくなったら甘スロ専用の裏切り枠を足す設計変更が要る

### U67 CloudFront の払い出しの粒 / 引き戻しの復活率(2026-08-15 / ユーザー体感2件)
「CloudFront RUSH のクレジット払い出しが多すぎる」「復活しすぎ」の2件を **一体で** 着地させた回。
- [x] **CF RUSH の1回の払い出しを 5〜50枚 の帯へ**(ユーザー指示)。
  毎ゲームのキャッシュヒット 25/40/50/60枚 → **5/10/15/20/30/50枚**(90%が5〜20枚)/
  レア役の確定クレジット 弱チェ6〜ゴースト300枚 → **弱チェ10 / 強チェ30 / 確定役50枚**。
  減った期待値は **ヒット率 0.85 → 0.95** と **固定8G → 18G** で返し、
  RUSH 1回は 中央値270→**279** / 平均274→**281** / p99 566→**405枚** と他3種と横並びを維持
- [x] **確定役(サメ/ゴースト)も帯の内側(50枚)に入れた**。
  「1ゲーム100枚の壁」の確定役例外を使わずに満たせる形(ヒット50 + 確定役50 = ちょうど100枚)。
  結果、**100枚超を作るチャネルはヒーローRUSHだけ**になった(0.005 → 0.001回/セッション)
- [x] **引き戻し 0.82 → 0.55**(U50 以前の値へ差し戻し。復活 0.25 → **0.16回/セッション**)。
  U50 の「上振れを連チャンで作る」は数字では成立していたが、82% は5回に4回戻るため
  引き戻し層が「当落を見る画面」ではなく「戻るのを待つ画面」になっていた
- [x] **減らした復活の回数を「1回の価値」で返した**: 復帰先を
  **ゴーストボーナス(RUSH 45%)→ ゴーストボーナスSP(85%)** へ(`RECOVERY_BONUS`)。
  復活1回の価値 ≒ 199 → **357枚** = 「復活しにくいが、復活したらラッキー」。
  ※ **successRate と RECOVERY_BONUS.bonusId は必ずセットで動かすこと**。
  実測で確認済み: 0.55 のまま復帰先を据え置くと 上位1% は 1,144〜1,180枚 で目標(1,250枚超)に届かない
- [x] 実測(50,000セッション / seed=20260814): 平均 **239.1枚** / 中央値 **132枚** /
  上位1% **1,314枚** / 機械割 **179.7%** / 初当り **1/100.3**。5シードで全項目レンジ内
- [ ] **副作用(要判断)**: ボーナス内訳が シャーク37.9% / ゴースト23.1% / **ゴーストSP 39.0%** となり、
  最上位ボーナスが最頻になった(U67 前は SP 25.6% / ゴースト 39.2%)。
  引き戻し由来のボーナスが全部 SP になるため。気になるなら復帰先を戻すのではなく
  **SP の直撃振り分け(`DIRECT_BONUS_DIST`)側を絞る**のが筋

### U50 バランス総点検(2026-08-15 / ユーザー指示「Auroraラッシュとか純増多すぎてやばすぎ」)
平均値合わせではなく **分布**(1ゲームの重さ / 上振れの尻尾)を整えた回。
- [x] **1ゲーム100枚の壁**: フリーズ恩恵と確定役(サメ/ゴースト)以外で1ゲーム100枚超を作らない。
  Aurora の ACU 上限 150 → **70枚**(旧仕様はハズレのゲームでも150枚出ていた)/
  CloudFront RUSH の1ゲーム最大 800 → **60枚**(ヒット率 0.55 → 0.85 で薄く広く)/
  ヒーローRUSH の +α 最大 1750 → **500枚**(非確定役は30枚)/
  CloudFront・Kinesis ゾーンの1ゲーム上乗せ 200・150 → **60枚**。
  実測の1ゲーム純増は 中央値35 / p99 75 / p99.9 100枚、100枚超は 0.003回/セッション(全部確定役)
- [x] **RUSH 1回の獲得を4種そろえた**(中央値150〜300 / 平均250〜400 / **p99 800枚以下**)。
  AS 210/307/770 · CF 270/272/505 · Aurora 240/342/724 · ヒーロー 280/289/404。
  ゲーム数が伸びる AS と Aurora に **通算上限**(22G / 15G)を新設し、発散する尻尾を断った
- [x] **上振れは連チャンで作る**: 引き戻し 0.55 → **0.82**(復帰先のゴーストボーナスは据え置き)。
  「1回のRUSHでセッションが決まる」から「何度も繋がった回が伸びる」へ
- [x] `scripts/channel-probe.mjs` 追加(チャネル別の寄与度 / 1ゲーム純増の分布 / RUSH 1回の分位点)
- [ ] **上位1%は 1,288枚**(現行目標 1,250枚超は達成 / 旧目標 1,600枚には届かない・ユーザー判断待ち)。
  100回転に入る RUSH は連鎖しても4〜5回が限界なので、RUSH 1回を p99 800枚で頭打ちにすると
  上位1%には約1,300枚の算術的な天井ができる(平均を上げて届かせようとすると機械割が200%に乗る)。
  1,600枚へ戻すなら **RUSH 1回の p99 を 1,100枚まで緩める** か
  **機械割の上限を 200% まで緩める** かの二択。`scripts/sim.mjs` の `TARGET` に同じ説明あり

### 検証指摘の全件対応(2026-08-15 / つばき10件 + しおん22件)
しおんのテストプレイ + つばきのレビューで挙がった32件を、minor 込みで一括修正した回。
- [x] **「ハズレを名乗る予告」の穴を塞いだ**(major / つばき1・しおん F1)。
  `yokoku-bedrock.js` / `batch3` / `batch4` の LOSE_WHEN・WEAK_WHEN に
  `modeState.subState: ['COLD_START']` と `freeze: [false]` を追加。
  **通常時に当選が生まれる4経路(成立役 / ステージ毎G抽選 / 天井 / レバーONフリーズ)を全部塞いだ**
  形になり、文字化けやガセ予告が「実は当選しているゲーム」で出ることが構造的に無くなる。
  ブラウザ実測 1,269スピンで、ハズレ系の発火は **15回すべて COLD_START**(高確・激アツ 0回)
- [x] **Bedrock 生成演出が埋もれていた**(major / しおん F2)。`SCENARIO_WEIGHT_BOOST` の
  `yw_wind_*` / `yw_coldstart_*` を **×55 → ×20** に下げ、`ybr_gen_*` に **×25**
  (ハズレ系は ×8)を新設。レア役プールの取り分が 0.3〜0.5% → **8.6〜15.7%** になり、
  ブラウザ実測でも 1,269スピンで ybr_* が **23回**(修正前は 1,200スピンで 0回)
- [x] 盤面の嘘・読めない表示: CZ の `cw_meter_swing` に `label:'ERROR BUDGET'` を明示(F3。
  既定値の `CPU UTIL` が出て盤面上部と別名の指標が2つ並んでいた。GameDay 側と前兆の請求メーターも同じ穴があったので一緒に直した)/
  SHIELD の波名に `short`(短縮名)を持たせてカード幅で自動切替(F4)/
  CONFIG_RULES の状態名を `statusLabels` で **NON_COMPLIANT / 評価中 / COMPLIANT** へ(F5。
  NG・WARN は AWS Config に存在しない語だった)/ 「GREEN n / N で突破」を
  「COMPLIANT n / N — あと m」へ(F21)/ 結果告知の頭で `lcd.windDown` を呼んで
  途中経過のゲージを畳む(F12。新アクション `lcd.windDown` を actions.js に追加)
- [x] 進行アナウンスが落ちる件(F14): 新CZ3種の突入シナリオの `duration` を短縮して
  visual 枠を早く明け渡し、`cz_shield_mitigated` に `budgetExempt` と `priority:'cutin'` を付けた。
  ヘッドレスの調停プローブで **Config 3/3・DX 1/1・Shield 6/6 + 完成の画** が全部通るようになった
  (director に `budgetExempt` を新設。**1ゲームに何度も飛ぶイベントには付けないこと**)
- [x] DX の開通工程が2本しか出ていなかった(つばき6): 工程名を「ゲーム番号」ではなく
  **「テロップを出した回数」**で進めるようにし、`linkSteps` も実際に出る3本へ揃えた
- [x] リザルト: 到達率(RANKS)を U48 後の実測へ更新(F6。甘スロ E が表示16%対実測9.3%だった)/
  買い取り明細は `buyout > 0` のときだけ出す(F17)/ 開いた後のテロップを
  「R キー / ↑キーでもう一度」へ(F18)/ `perGame` の小数を丸めた
- [x] UI: デバッグ凡例の状態行が切れる(F7 / 高さ236pxへ、はみ出さないよう上へ伸ばした)/
  HOW TO PLAY に目的の1行、スマホ説明に操作方法、狭い横長の窓だけに出る最小ヒントを新設(F8)/
  甘スロ切替は進行中のときだけ confirm(F9)/ 座布団の重なり回避(F19)/
  デバッグ状態行と `AWSLOT.isInputMasked()` で入力ガードを観測可能に(F22)
- [x] クイズ2問を自分のルール違反から救出: `egress` は宛先をインターネットに限定(F10 / 条件①)、
  `ml` は SageMaker 固有の論点へずらして Bedrock との鏡写しを解消(F11 / 条件③)
- [x] 入力の二重発火ガードを **要素一致**まで見るように(つばき10)。
  デバッグキーの直後の Tab+Enter が黙って落ちていた
- [x] 陳腐化コメント・ドキュメント: 信頼度表と `YOKOKU_CHANCE_SCALE` の写しを全廃して
  「data/modes.js・director.js を見ること」へ(つばき2・3)/ U48 の相殺倍率を 0.60倍で統一(つばき4)/
  Shield の `recoverAmount` を削除(つばき5)/ `main.js` の陳腐化2件(つばき8)/
  DESIGN.md の M04・実測値、`freeze.js` の合成確率(つばき9)/
  docs 3ファイルの台名と画像生成プロンプトを JAWSLOT へ(F20)
- [x] フリーズ冒頭の黒画面に、極小の地鳴りを1つだけ置いた(F15。文字も動きも足していないので溜めは不変)
- [ ] **OGP画像 2.4MB の圧縮(未対応 / assets の画像は変更禁止)**(F16)。
  X / Slack / Discord のプレビュー生成はおおむね1MB前後で頭打ちになる。
  `pngquant --quality=65-85 assets/ogp.png` か JPEG(品質85)への差し替え+`og:image` の付け替えが要る。
  画像そのものを差し替えてよいかはユーザー判断

### U53・U54(2026-08-15 / ユーザー指示)
- [x] **U53 クイズルーレット廃止 → 「最初に止めるリール」3択**(`data/scenarios/quiz.js` を全面差し替え / `qz_reelpick_*` 3本)。
  「どのリールから止める?」の3択を出し、**プレイヤーが実際に最初に止めたリール**を答えにする
  (新しい選択UIは作らず、既存の停止操作をそのまま入力に使う。`{ waitFor:'stop1', params:{ pick:'$stop1.index' } }`)。
  どのリールが正解だったかは結果に合わせた**後決め**(実機の押し順当てと同じ / ユーザー明示了承済み)で、
  **出目・当落はレバーON時点の確定値のまま1枚も動かない**(`compare-drivers.mjs` 全ドライバ一致で確認)。
  「正解=当選」の担保は when 条件だけ(正解版=`modeEnter: CZ` / 不正解版=`zencho_end: MISS` と構造的非当選のレバーON)。
  発生枠・weight・chance はクイズ時代の実測値を引き継いだので演出の総量は不変。U42(背景の当落バレ防止)は
  `STAGE_HOLD_ANIMS` に `reel_pick_choice` を登録して維持(hold は**モードが変わる正解版だけ**。
  不正解版に付けると次ゲームの第1停止まで投入・レバーが塞がる)。デバッグは `?rp=`(1 / entry_cz / miss / miss_idle)。
  **クイズの46問データ(`data/quiz.js`)と盤面(`aws_quiz_roulette`)は休止中のまま保全**(戻すときはシナリオを書き戻すだけ)
- [x] **U54 「揃えろ」画面に実絵柄**(`render/lcd.js` の `_drawBonusReady`)。
  「ゴースト7を揃えろ!」「サメBARを揃えろ!」のプレート3枚に、文字(7 / BAR)ではなく
  **リールに乗っている絵柄そのもの**(`assets/symbols/GHOST7.png` / `SHARKBAR.png`)を出す。
  経路は絵柄飛来予告と同じ共有ストア `symbolAssets` で、初回だけ段階縮小して焼き付ける。
  未ロード時は従来の文字プレートへフォールバック。帯が出ていないときだけ 54×50 → 60×56 に拡大(下端は 4px だけ下がる)

### U63 レア役の基準をさらに一段UP(2026-08-15 / ユーザー指示)
「通常モードを、いまの甘スロ相当にしてほしい」= **U48 の値をさらに2倍(従来比4倍)**。
甘スロ(`?ama=1`)は倍率2のままなので **従来比8倍**(通常より更に甘い)を維持。
- [x] **レア役 1/12.3 → 1/6.17**(`data/flags.js`): 弱チェ **1/12.5** / スイカ **1/25** /
  チャンス目 **1/45** / 強チェ **1/62.5** / サメ **1/300** / ゴースト **1/1500**。
  甘スロは更に2倍で **1/3.1**(テーブル合計 0.686 で 1 を超えない)
- [x] **相殺(U48 と同じ「1回あたりの変換率を絞る」手法)**:
  CZ当選(`CZ_ENTRY.table`)0.5倍 / ステージ昇格 0.7倍 / `czPerGame` **0.54倍** /
  フリーズ率(レア役の行)0.5倍 + 共通役 0.00060 → 0.00065 / 派生ゾーン当選 0.5倍 /
  RUSH 4種の上乗せは **非確定役だけ 0.5倍**(AS 1/2/2/3・CF 6/10/10/20・
  Aurora 7/9/9/14・ヒーロー 5/9/9/15。**確定役=サメ・ゴーストは据え置き**)
- [x] **長さで相殺した2つ**: Well-Architected CZ 9G → **7G**(6本積める確率 94.0% → 83.4% / DP)、
  Aurora RUSH 8G → **7G**(`addGamePerWin` がレア役比例で伸びて 1回の中央値が 319枚になっていた)
- [x] **コイン持ちの戻し**: 通常時のベル払出 8 → **7枚**。レア役ぶんの払出が +0.223枚/G 増えるので、
  ベル1枚(−0.2枚/G)で戻して通常時の純増を U48 と同じ **−0.66枚/G** に据え置いた
- [x] **中央値の戻し**: 天井 75G → **78G**。天井は「引けなかった回に必ず配られる1回」で
  **分布のちょうど真ん中だけに効く**(上位1%は1枚も動かない)
- [x] 実測(20,000セッション × 6シード): 初当り **1/97.9〜100.6** / 平均 **237〜247枚** /
  中央値 **144〜162枚** / 上位1% **1,282〜1,315枚** / 上位0.1% 1,638〜1,670枚 /
  機械割 **179.1〜182.4%** / フリーズ **9.1〜10.1%** / 1ゲーム100枚超は確定役のみ 0.003〜0.005回。
  RUSH 1回は AS 245/303 · CF 270/273 · Aurora 281/306 · ヒーロー 288/296(中央値/平均・p99 全て800枚以下)。
  `sim.mjs --session` は通常・甘スロとも **PASS**、`compare-drivers.mjs` は全ドライバ一致
- [x] リザルトの到達率(`game/modes/result.js` の RANKS)を新分布の実測へ更新
  (通常 0.4/3/8/24/18/26/21% · 甘スロ 2/7/13/24/17/33/3%。甘スロは E が 9% → **3%**)
- [x] `scripts/balance-probe.mjs` に **分位点(p10/p25/p75/p90/p97/p99.5)** と
  **通常時のステージ滞在率** を追加(称号の閾値決めと「高確が日常になっていないか」の点検用)
- [x] **称号(`src/data/titles.js`)を作成**(U56)。閾値は実測の分位点どおり
  常時 / **10** / **158** / **384** / **650** / **1010** / **1450**枚 で、
  実測の到達率は 100% / 82.5% / 50.8% / 27.6% / 12.1% / 3.7% / 0.26%。
  RANK(`game/modes/result.js`)とは別枠で、リザルトに1行だけ静かに出す
- [x] **甘スロの初当りが 1/62 で頭打ち**だった件 → **U72 のCZ導線一本化で解消**。
  CZ供給の主線が「上位ステージの滞在率 × `czPerGame`」から **チャンス目(甘スロでは 1/22.5)** へ移ったので、
  レア役の倍率がそのまま初当りへ効くようになった(甘スロの実測は 1/53 前後)。
  ステージ滞在率に縛られる構造そのものが無くなったため、`czPerGame` の甘スロ専用係数は不要になった
- [ ] **ルナカメオの頻度按分(担当範囲外のまま / `scenarios/**` は変更禁止)**。
  U48 で 1/540 → 1/272 になっていたところに U63 が乗って **1/136** まで来ている。
  1/400〜600 に戻すには `yokoku-luna.js` の chance を **現行の 1/4** へ
  (スイカ 0.040→0.010 / 弱チェ 0.030→0.0075 / 強チェ 0.060→0.015 /
  チャンス目 0.045→0.011 / 確定役 0.350→0.088)
- [ ] **通常時のステージ滞在が 通常44% / 高確42% / 激アツ15%** になった(U48 前は通常が5割強)。
  レア役4倍に対して昇格を 0.7倍しか絞っていないため。
  **U72 でCZの主線がチャンス目へ移り、ステージの毎G抽選は 0.3% / 1.2% のおまけになった**ので
  「高確が日常」でも初当りは動かなくなった(実測のCZ突入でステージ経由は 7.8%)。
  ただし **ステージが上がっても嬉しいことがほぼ無い**状態でもあるので、
  ステージの役割を作り直すなら「昇格をもう一段絞って `czPerGame` を上げる」組み替えになる

### 学習コンテンツ強化(2026-08-15 / AWS図鑑・学習ログ)
既にある学習素材(クイズ46問・**豆知識99件**)を「その場で消える読み物」から
**積み上がる記録**へ変える。道中のテンポは変えず、**増やすのはリザルトだけ**。
> 豆知識はこの回の着手時点で82件。クイズの正解が全件図鑑に載るよう17件足して
> **99件**になった(`AWS_TRIVIA.length` が正。画面の総数はここから導出していて直書きしない)。
- [x] **`src/data/services.js` 新規**: 分類9種(`CATEGORIES`)と
  **クイズ正解名 → 豆知識の正式名 46件**の対応表(`QUIZ_ANSWER_TO_SERVICE`)。
  **部分一致で解決しない**(`Lambda`→関数URL / `Shield`→Advanced / `Redshift`→Serverless /
  `SageMaker`→HyperPod のような上位プラン・派生機能への誤マッピングを防ぐため全件を明示列挙)。
  `DEX_TOTAL` は `AWS_TRIVIA.length` から導出。読み込み時に自己点検(`verifyServiceMap`)
- [x] **`AWS_TRIVIA` 82 → 99 件**(`data/quiz.js`): 全件に `cat` を付与し、
  クイズの正解のうち載っていなかった **17件を追加**(ALB / ECS / GameLift Servers / Cognito /
  Config / MSK / IoT Core / CDK / Shield / Redshift / EFS / EC2 Auto Scaling / Organizations /
  Systems Manager / Transcribe / SageMaker AI / NAT ゲートウェイ)。
  `oneLiner` はクイズの設問の論点をそのまま言い切る形で、
  **版によって伸び縮みする数値は1件も名乗らない**
  (Shield の「L3/L4」は OSI の層の名前なので数値には数えない)。
  `TRIVIA_RECENT_MAX` の根拠コメントも 82 → 99 へ更新
- [x] **`src/data/learnlog.js` 新規**: localStorage(`awslot.learn.v1`)へ
  出会ったサービスとクイズの正誤を積む。セッション集計はメモリのみ。
  **localStorage / location が無い Node でも例外を出さない**
  (Node 25 は `localStorage` を持つので `window` の有無も見る)。
  容量超過・プライベートモードの例外は静かに握りつぶしてメモリだけで動き続ける
- [x] **豆知識カードの強化**(`lcdanims-extra.js`): カテゴリピル / NEW / `AWS図鑑 n/99`。
  記録は `triviaStateOf` に1か所だけ(`params.__t` のキャッシュで1再生1回)。
  カード下端は従来どおり **液晶 y146 以内**(カウンタは y136)
- [x] **クイズ盤面の足元にカテゴリ**(full版のみ / 判定後だけ「第1停止が答え」と差し替え)。
  compact(当選版)は下限に余裕が無いので**行を増やさない**
- [x] **記録キュー** `layer:'learn' / action:'quizResult'` を3シナリオへ1本ずつ。
  画も音も出さず、当落・出目・スコアには影響しない
- [x] `?learn=off` で学習の記録と表示を全部止める / `?learnreset=1` で記録を消す(Hキー凡例へ追記)
- [x] 回帰: `compare-drivers.mjs` 4ドライバ全一致・例外なし / `sim.mjs --session=300 777` PASS
- [x] **L5 リザルトの学習サマリー**(`render/resultpanel.js` + `style.css`):
  スコアの内訳の下に折りたたみ(`<details>`)で「今日出会ったサービスのチップ /
  クイズ成績(このセッション・通算)/ 苦手カテゴリ / 図鑑の進捗 / 学習バッジ」を出す。
  **道中の表示は1つも増やしていない**。閉じたままでも読めるよう見出しに要約
  (`学習サマリー — 今日 n件 / クイズ n問正解 / 図鑑 n/99`)を出し、
  1件も記録が無いセッションでは折りたたみごと隠す
- [x] **L6 学習バッジ7種**(`data/learnlog.js` の `getBadges`): 図鑑10/30/60/コンプリート +
  クイズ通算10問正解 / 50問正解 / 正答率70%(20問以上)。
  **スコアの称号(`data/titles.js`)とは完全に別枠**で、名前も体系も被らせない
  (混ぜると「画面の到達率が `sim.mjs` の実測と食い違う」事故になる)
- [x] **L7 公式ページへの誘導**(`data/services.js` の `SERVICE_DOCS`):
  サマリーのサービス名を公式ページのリンクにする
  (`target="_blank"` + `rel="noopener noreferrer"` + 「新しいタブで開く」旨の `aria-label`)。
  **主要15件だけ**に絞っている。全99件ぶん持つと改名・統合のたびに死んだリンクが混ざるため、
  持っていないサービスはリンクにせずただの文字で出す
- [x] 椿レビュー(検収)8件対応: **GameLift → Amazon GameLift Servers**(ファミリー名と
  製品名の取り違え。`AWS_TRIVIA` と `QUIZ_ANSWER_TO_SERVICE` の両方)/
  `learn.quizResult` に**砦**を追加(盤面が出題していないときに `reelPickVerdictOf` を呼ぶと
  **架空の出題が生まれて偶然の正誤が永久保存される**ため、`inspectReelPickState()` が null なら
  何もしない。加えて出題ごとの `__recorded` で二重計上を止めた)/
  AWS Shield の1行を「L3/L4 の DDoS を標準(Standard)で追加費用なく防ぐ」へ /
  `recordQuizAnswer` は `QUIZ_BY_ID` に無い問題idを記録しない /
  図鑑カウンタの `getDexProgress()` を毎フレーム → 1再生1回(`params.__t` に同居)/
  表記の要注意に Application Load Balancer(ELB の機能・接頭辞なし)を追記 /
  1行の長さの目安を実測へ(fitFont 16→13px で全角26字まで)/ この台帳の実装ズレ解消
- [ ] 将来案: 図鑑ビューア画面 / 予告シナリオ505本への `service` タグ付け /
  豆知識の深掘り1行 / 公式ドキュメントURLの全件整備(いまは主要15件のみ)

## 検証(M/P完了後)
- [ ] しおん一括E2E: テロップが読めるか/100回転→買い取り→リザルト/3択リール/新ステージ/UI素材の見た目/回帰
- [x] FLOW.mdの更新(BONUS_READY、100回転スコアアタック、引き戻し1段化、前兆/擬似連/3択リール反映。
  全体遷移図・セッションのライフサイクル図・1ゲームの流れ・モード早見表を全面書き直し。mermaid-cliで全ブロックのレンダリング検証済み)
- [x] **FLOW.md の U72/U73 追従(2026-08-16)**: CZ **11種**の内訳表を新設 /
  導線を「チャンス目・サメ揃い = CZ確定」へ書き換え / RUSH 4種を現行値(AS 15枚・通算52G /
  CF 5〜50枚の帯 / Aurora 初期7G / ヒーロー 50%×50枚)へ / 引き戻し 0.55 + 復帰先ゴーストボーナスSP /
  レバーONフリーズの現行仕様(役別の発生率・恩恵・実測9.3%)を表の注記へ /
  実測値の表を `sim.mjs --session=5000` の現行値へ差し替え /
  **「6. 前兆と赤文字予兆」章を新設**(前兆の本/ガセ・赤帯4系統の信頼度表)。mermaid 5ブロックとも再レンダリング確認済み
- [ ] DESIGN.mdの更新(BONUS_READY、スコアアタック、改名反映)
- [ ] コミット

## ユーザー側(任意)
- [ ] C-2 操作系グリッド画像(ボタン点灯差分) — 筐体絵で代替可能なため任意
- [ ] B-1 キャラ立ち絵グリッド(カットイン用3表情×2) — 現状プロシージャルで代替中
- [ ] 音声生成: awslot/.env に AIVIS_CLOUD_API_KEY と Kiro/ジョージの model_uuid を設定して scripts/generate-voices.mjs 実行
